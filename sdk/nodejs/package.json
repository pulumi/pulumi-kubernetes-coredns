{
    "name": "@pulumi/kubernetes-coredns",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource kubernetes-coredns ${VERSION}"
    },
    "dependencies": {
        "@pulumi/kubernetes": "^3.7.1"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
